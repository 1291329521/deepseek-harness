/**
 * Browser half entry: the annotation resolver, hover overlay, explain
 * shortcut, and settings card register here. Export discipline:
 * packages/client/AGENTS.md.
 * @module @deepseek-ai/dsh-client-ui-terminology/src/client
 */
import type { Context } from '@deepseek-ai/cordis'

/** Called when the Web loader activates the plugin; contributes nothing until the browser tasks land. */
export function apply(_ctx: Context): void {}
