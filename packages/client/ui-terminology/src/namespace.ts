/**
 * The single key the Host settings registration and the browser registrations
 * join on. It carries no schema runtime, so the browser half can reach it
 * without pulling a validator into its bundle.
 * @module @deepseek-ai/dsh-client-ui-terminology/src/namespace
 */

/** Settings namespace owned by terminology; also the slot key the card registers under. */
export const TERMINOLOGY_NAMESPACE = 'terminology'
